<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <!-- Inherit Sale Order Form View to add Custom Order Date -->
        <record id="view_order_form_custom_date" model="ir.ui.view">
            <field name="name">sale.order.form.custom.date</field>
            <field name="model">sale.order</field>
            <field name="inherit_id" ref="sale.view_order_form"/>
            <field name="arch" type="xml">
                <!-- Add custom_order_date field after date_order in the header -->
                <xpath expr="//field[@name='date_order']" position="after">
                    <field name="custom_order_date" 
                           optional="show"
                           widget="date"/>
                </xpath>
            </field>
        </record>
    </data>
</odoo>